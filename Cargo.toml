[package]
name = "dolbaeb-simulator"
version = "0.1.0"
edition = "2024"

[dependencies]
macroquad = "0.4.15"
macroquad-tiled = "0.2.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
opt-level = 3
lto = true
panic = 'abort'
